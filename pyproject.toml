[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
[project]
name = "landlaw-ai-mcp"
version = "1.0.0"
description = "MEOK AI Labs — UK property law & Land Registry MCP Server"
readme = "README.md"
license = {file = "LICENSE"}
requires-python = ">=3.10"
authors = [{name = "MEOK AI Labs", email = "nicholas@meok.ai"}]
keywords = ["mcp", "legal", "land-registry", "property", "conveyancing", "uk-law", "meok"]
dependencies = ["mcp>=1.0.0"]
[project.urls]
Homepage = "https://landlaw.ai"
Repository = "https://github.com/CSOAI-ORG/landlaw-ai-mcp"
